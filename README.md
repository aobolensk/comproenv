# comproenv
Environment for competitive programming

Requirements:
- Python 3.7+ (recommended)

Build and run:

```console
# Via script:
$ python launch.py --f build run

# Alternative:
$ mkdir build
$ cd build
$ cmake ..
$ cmake --build .
$ ./bin/comproenv
```
